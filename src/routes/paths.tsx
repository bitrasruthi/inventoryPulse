export enum paths {
  HOME = "/",
  LOGIN = "/login",
  INSPECTIONS = "/inspections",
  INSPECTIONSDETAILS = "/inspection/:id",
  PROPERTIES = "/properties",
  USERS = "/users",
  CLIENTS = "/clients",
}
