export enum paths {
  HOME = "/",
  LOGIN = "/login",
  INSPECTIONS = "/inspections",
  INSPECTIONSDETAILS = "/inspections/inspectionsDetails/:id",
  PROPERTIES = "/properties",
  USERS = "/users",
  CLIENTS = "/clients",
}
